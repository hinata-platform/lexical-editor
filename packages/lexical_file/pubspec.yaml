name: lexical_file
description: >-
  The .lexical document envelope for lexical_core: editorState, lastSaved,
  source and version, wire-compatible with @lexical/file. Pure Dart.
version: 1.7.4
repository: https://github.com/hinata-platform/lexical-editor
issue_tracker: https://github.com/hinata-platform/lexical-editor/issues

environment:
  sdk: ^3.8.0

resolution: workspace

dependencies:
  lexical_core: ^1.7.0
  meta: ^1.15.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
