# Example — lexical_rich_text

A console program showing what lexical_rich_text does, with the output explaining
itself as it goes.

```sh
cd packages/lexical_rich_text
dart run example/main.dart
```

No Flutter binding, no device, no widgets: this package is pure Dart, and the
example proves it by running under a plain Dart VM.
