name: lexical_conformance
description: >-
  Test-only package that registers every node type and verifies the whole
  fixture corpus round-trips. Not published.
publish_to: none

environment:
  sdk: ^3.8.0

resolution: workspace

dependencies:
  lexical_code: ^1.0.0
  lexical_core: ^1.0.0
  lexical_hashtag: ^1.0.0
  lexical_history: ^1.0.0
  lexical_link: ^1.0.0
  lexical_list: ^1.0.0
  lexical_mark: ^1.0.0
  lexical_rich_text: ^1.0.0
  lexical_table: ^1.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
